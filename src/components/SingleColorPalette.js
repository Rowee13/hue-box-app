// import { useState } from "react";

// import ColorBox from "./ColorBox";

// ________________________________________________________________

const SingleColorPalette = ({ palettes, colorToFilterBy }) => {
  // const [colorShades, setColorShades] = useState([]);

  // const gatherShades = () => {
  //   let allColors = palettes.colors;

  //   for (let key in allColors) {
  //     palettes.concat(
  //       allColors[key].filter((color) => color.id === colorToFilterBy)
  //     );
  //   }
  //   return colorShades.slice(1);
  // };

  // const colorBoxex = colorShades.map((color) => (
  //   <ColorBox key={color.id} name={color.name} color={color.hex} />
  // ));

  return (
    <div>
      <h1>SingleColorPalette</h1>
    </div>
  );
};

export default SingleColorPalette;
